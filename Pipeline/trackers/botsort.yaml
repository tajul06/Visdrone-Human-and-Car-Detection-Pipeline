tracker_type: botsort

track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
track_buffer: 30
match_thresh: 0.8

fuse_score: true

# BoT-SORT settings
cmc_method: orb
proximity_thresh: 0.5
appearance_thresh: 0.25

# Optional re-id (disabled by default)
with_reid: false
